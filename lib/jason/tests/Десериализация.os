// BSLLS:DuplicateStringLiteral-off
// BSLLS:MagicNumber-off
#Использовать ".."
#Использовать "."
#Использовать asserts

&Тест
Процедура ДесериализоватьПростойОбъект() Экспорт
	
	// Дано
	Строка = "
	|{
	|	""number"": 1,
	|	""Строка"": ""значение"",
	|	""Массив"": [1, 2],
	|	""Структура"": {
	|		""Поле1"": ""значение1"",
	|		""Поле2"": ""значение2""
	|	},
	|	""Ссылка"": {
	|		""number"": 2
	|	}
	|}";
	
	ДесериализаторJson = Новый ДесериализаторJson();

	// Когда
	Объект = ДесериализаторJson.Десериализовать(Строка, Тип("ТестовыйКласс"));

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.Число).Равно(1);
	Ожидаем.Что(Объект.Строка).Равно("значение");

	Ожидаем.Что(Объект.Массив).ИмеетТип("Массив");
	Ожидаем.Что(Объект.Массив).ИмеетДлину(2);
	Ожидаем.Что(Объект.Массив[0]).Равно(1);
	Ожидаем.Что(Объект.Массив[1]).Равно(2);

	Ожидаем.Что(Объект.Структура).ИмеетТип("Структура");
	Ожидаем.Что(Объект.Структура.Поле1).Равно("значение1");
	Ожидаем.Что(Объект.Структура.Поле2).Равно("значение2");

	Ожидаем.Что(Объект.Ссылка).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.Ссылка.Число).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияПользовательскогоТипа_МассивОбъектовВСтруктуру() Экспорт

	// Дано
	JSON = "
	|{
	|  ""МассивСтруктур"": [
	|    { ""x"": 1 },
	|    { ""y"": 2 }
	|  ]
	|}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Объект = Десериализатор.Десериализовать(JSON, Тип("ТестовыйКласс"), Ложь);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.МассивСтруктур).ИмеетТип("Массив");
	Ожидаем.Что(Объект.МассивСтруктур).ИмеетДлину(2);
	Ожидаем.Что(Объект.МассивСтруктур[0]).ИмеетТип("Структура");
	Ожидаем.Что(Объект.МассивСтруктур[0].x).Равно(1);
	Ожидаем.Что(Объект.МассивСтруктур[1]).ИмеетТип("Структура");
	Ожидаем.Что(Объект.МассивСтруктур[1].y).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияПользовательскогоТипа_МассивОбъектовВСоответствие() Экспорт

	// Дано
	JSON = "
	|{
	|  ""МассивСтруктур"": [
	|    { ""x"": 1 },
	|    { ""y"": 2 }
	|  ]
	|}";

	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Объект = Десериализатор.Десериализовать(JSON, Тип("ТестовыйКласс"), Истина);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.МассивСтруктур).ИмеетТип("Массив");
	Ожидаем.Что(Объект.МассивСтруктур).ИмеетДлину(2);
	Ожидаем.Что(Объект.МассивСтруктур[0]).ИмеетТип("Соответствие");
	Ожидаем.Что(Объект.МассивСтруктур[0]["x"]).Равно(1);
	Ожидаем.Что(Объект.МассивСтруктур[1]).ИмеетТип("Соответствие");
	Ожидаем.Что(Объект.МассивСтруктур[1]["y"]).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияПользовательскогоТипа_МассивОбъектовВПользовательскийТип() Экспорт

	// Дано
	JSON = "
	|{
	|  ""МассивПользовательскихОбъектов"": [
	|    { ""number"": 1 },
	|    { ""number"": 2 }
	|  ]
	|}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Объект = Десериализатор.Десериализовать(JSON, Тип("ТестовыйКласс"), Ложь);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.МассивПользовательскихОбъектов).ИмеетТип("Массив");
	Ожидаем.Что(Объект.МассивПользовательскихОбъектов).ИмеетДлину(2);
	Ожидаем.Что(Объект.МассивПользовательскихОбъектов[0]).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.МассивПользовательскихОбъектов[0].Число).Равно(1);
	Ожидаем.Что(Объект.МассивПользовательскихОбъектов[1]).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.МассивПользовательскихОбъектов[1].Число).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияПользовательскогоТипа_СтруктураОбъектовВПользовательскийТип() Экспорт

	// Дано
	JSON = "
	|{
	|  ""СтруктураПользовательскихОбъектов"": {
	|    ""Объект1"": { ""number"": 1 },
	|    ""Объект2"": { ""number"": 2 }
	|  }
	|}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Объект = Десериализатор.Десериализовать(JSON, Тип("ТестовыйКласс"), Ложь);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.СтруктураПользовательскихОбъектов).ИмеетТип("Структура");
	Ожидаем.Что(Объект.СтруктураПользовательскихОбъектов.Объект1).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.СтруктураПользовательскихОбъектов.Объект1.Число).Равно(1);
	Ожидаем.Что(Объект.СтруктураПользовательскихОбъектов.Объект2).ИмеетТип("ТестовыйКласс");
	Ожидаем.Что(Объект.СтруктураПользовательскихОбъектов.Объект2.Число).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_ОбъектВСоответствие() Экспорт

	// Дано
	JSON = "{""a"": 1, ""b"": 2}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Объект = Десериализатор.Десериализовать(JSON, Неопределено, Истина);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("Соответствие");
	Ожидаем.Что(Объект["a"]).Равно(1);
	Ожидаем.Что(Объект["b"]).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_МассивВСоответствие() Экспорт

	// Дано
	JSON = "[1, ""a"", true, {""x"": 3}]";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	МассивЗначений = Десериализатор.Десериализовать(JSON, Неопределено, Истина);

	// Тогда
	Ожидаем.Что(МассивЗначений).ИмеетТип("Массив");
	Ожидаем.Что(МассивЗначений[3]).ИмеетТип("Соответствие");
	Ожидаем.Что(МассивЗначений[3]["x"]).Равно(3);

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_МассивВСтруктуру() Экспорт

	// Дано
	JSON = "[1, ""a"", true, {""x"": 3}]";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	МассивЗначений = Десериализатор.Десериализовать(JSON, Неопределено, Ложь);

	// Тогда
	Ожидаем.Что(МассивЗначений).ИмеетТип("Массив");
	Ожидаем.Что(МассивЗначений[3]).ИмеетТип("Структура");
	Ожидаем.Что(МассивЗначений[3].x).Равно(3);

КонецПроцедуры
&Тест
Процедура ДесериализацияБезТипа_Число() Экспорт

	// Дано
	JSON = "123";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON);

	// Тогда
	Ожидаем.Что(Значение).ИмеетТип("Число");
	Ожидаем.Что(Значение).Равно(123);

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_Строка() Экспорт

	// Дано
	JSON = """строка""";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON);

	// Тогда
	Ожидаем.Что(Значение).ИмеетТип("Строка");
	Ожидаем.Что(Значение).Равно("строка");

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_Массив() Экспорт

	// Дано
	JSON = "[1, ""a"", true, {""x"": 3}]";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	МассивЗначений = Десериализатор.Десериализовать(JSON);

	// Тогда
	Ожидаем.Что(МассивЗначений).ИмеетТип("Массив");
	Ожидаем.Что(МассивЗначений).ИмеетДлину(4);
	Ожидаем.Что(МассивЗначений[0]).Равно(1);
	Ожидаем.Что(МассивЗначений[1]).Равно("a");
	Ожидаем.Что(МассивЗначений[2]).Равно(Истина);
	Ожидаем.Что(МассивЗначений[3]).ИмеетТип("Структура");
	Ожидаем.Что(МассивЗначений[3].x).Равно(3);

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_Объект() Экспорт

	// Дано
	JSON = "{""a"": 1, ""b"": 2}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Объект = Десериализатор.Десериализовать(JSON);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("Структура");
	Ожидаем.Что(Объект.a).Равно(1);
	Ожидаем.Что(Объект.b).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияБезТипа_Null() Экспорт

	// Дано
	JSON = "null";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON);

	// Тогда
	Ожидаем.Что(Значение).Равно(Неопределено);

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневогоЧисла() Экспорт

	// Дано
	JSON = "123";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON, Тип("Число"));

	// Тогда
	Ожидаем.Что(Значение).ИмеетТип("Число");
	Ожидаем.Что(Значение).Равно(123);

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневойСтроки() Экспорт

	// Дано
	JSON = """строка""";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON, Тип("Строка"));

	// Тогда
	Ожидаем.Что(Значение).ИмеетТип("Строка");
	Ожидаем.Что(Значение).Равно("строка");

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневогоБулево() Экспорт

	// Дано
	JSON = "true";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON, Тип("Булево"));

	// Тогда
	Ожидаем.Что(Значение).ИмеетТип("Булево");
	Ожидаем.Что(Значение).Равно(Истина);

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневойДаты() Экспорт

	// Дано
	ДатаЗнач = Дата(2023, 1, 2);
	Сериализатор = Новый СериализаторJson();
	JSON = Сериализатор.Сериализовать(ДатаЗнач);
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	ПрочитаннаяДата = Десериализатор.Десериализовать(JSON, Тип("Дата"));

	// Тогда
	Ожидаем.Что(ПрочитаннаяДата).ИмеетТип("Дата");
	Ожидаем.Что(ПрочитаннаяДата).Равно(ДатаЗнач);

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневогоNull() Экспорт

	// Дано
	JSON = "null";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Значение = Десериализатор.Десериализовать(JSON, Тип("Неопределено"));

	// Тогда
	// JSON null читается как Неопределено в OneScript
	Ожидаем.Что(Значение).Равно(Неопределено);

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневогоМассива() Экспорт

	// Дано
	JSON = "[1, ""a"", true, {""x"": 3}]";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	МассивЗначений = Десериализатор.Десериализовать(JSON, Тип("Массив"));

	// Тогда
	Ожидаем.Что(МассивЗначений).ИмеетТип("Массив");
	Ожидаем.Что(МассивЗначений).ИмеетДлину(4);
	Ожидаем.Что(МассивЗначений[0]).Равно(1);
	Ожидаем.Что(МассивЗначений[1]).Равно("a");
	Ожидаем.Что(МассивЗначений[2]).Равно(Истина);
	Ожидаем.Что(МассивЗначений[3]).ИмеетТип("Структура");
	Ожидаем.Что(МассивЗначений[3].x).Равно(3);

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневойСтруктуры() Экспорт

	// Дано
	JSON = "{""a"": 1, ""b"": 2}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	СтруктураЗначений = Десериализатор.Десериализовать(JSON, Тип("Структура"));

	// Тогда
	Ожидаем.Что(СтруктураЗначений).ИмеетТип("Структура");
	Ожидаем.Что(СтруктураЗначений.a).Равно(1);
	Ожидаем.Что(СтруктураЗначений.b).Равно(2);

КонецПроцедуры

&Тест
Процедура ДесериализацияВложенныхПолей() Экспорт

	// Дано
	Строка = "
	|{
	|	""chat"": {
	|		""chatId"": 1,
	|		""title"": ""Тестовый чат"",
	|		""metadata"": {
	|			""description"": ""Описание""
	|		}
	|	},
	|	""ПростоеПоле"": ""значение""
	|}";
	
	ДесериализаторJson = Новый ДесериализаторJson();

	// Когда
	Объект = ДесериализаторJson.Десериализовать(Строка, Тип("ТестовыйКлассВложенные"));

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКлассВложенные");
	Ожидаем.Что(Объект.ИдЧата).Равно(1);
	Ожидаем.Что(Объект.НазваниеЧата).Равно("Тестовый чат");
	Ожидаем.Что(Объект.ОписаниеЧата).Равно("Описание");
	Ожидаем.Что(Объект.ПростоеПоле).Равно("значение");

КонецПроцедуры

&Тест
Процедура ДесериализацияВложенныхПолей_ОтсутствующийПромежуточныйОбъект() Экспорт

	// Дано - JSON без промежуточного объекта "chat"
	Строка = "
	|{
	|	""ПростоеПоле"": ""значение""
	|}";
	
	ДесериализаторJson = Новый ДесериализаторJson();

	// Когда
	Объект = ДесериализаторJson.Десериализовать(Строка, Тип("ТестовыйКлассВложенные"));

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКлассВложенные");
	Ожидаем.Что(Объект.ИдЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.НазваниеЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.ОписаниеЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.ПростоеПоле).Равно("значение");

КонецПроцедуры

&Тест
Процедура ДесериализацияВложенныхПолей_ЧитатьВСоответствие() Экспорт

	// Дано
	Строка = "
	|{
	|	""chat"": {
	|		""chatId"": 1,
	|		""title"": ""Тестовый чат"",
	|		""metadata"": {
	|			""description"": ""Описание""
	|		}
	|	},
	|	""ПростоеПоле"": ""значение""
	|}";
	
	ДесериализаторJson = Новый ДесериализаторJson();

	// Когда
	Объект = ДесериализаторJson.Десериализовать(Строка, Тип("ТестовыйКлассВложенные"), Истина);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКлассВложенные");
	Ожидаем.Что(Объект.ИдЧата).Равно(1);
	Ожидаем.Что(Объект.НазваниеЧата).Равно("Тестовый чат");
	Ожидаем.Что(Объект.ОписаниеЧата).Равно("Описание");
	Ожидаем.Что(Объект.ПростоеПоле).Равно("значение");

КонецПроцедуры

&Тест
Процедура ДесериализацияВложенныхПолей_ПромежуточныйОбъектNull() Экспорт

	// Дано - промежуточный объект "chat" равен null
	Строка = "
	|{
	|	""chat"": null,
	|	""ПростоеПоле"": ""значение""
	|}";
	
	ДесериализаторJson = Новый ДесериализаторJson();

	// Когда
	Объект = ДесериализаторJson.Десериализовать(Строка, Тип("ТестовыйКлассВложенные"), Истина);

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКлассВложенные");
	Ожидаем.Что(Объект.ИдЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.НазваниеЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.ОписаниеЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.ПростоеПоле).Равно("значение");

КонецПроцедуры

&Тест
Процедура ДесериализацияВложенныхПолей_ПромежуточныйОбъектПримитив() Экспорт

	// Дано - промежуточный объект "chat" является примитивом
	Строка = "
	|{
	|	""chat"": 42,
	|	""ПростоеПоле"": ""значение""
	|}";
	
	ДесериализаторJson = Новый ДесериализаторJson();

	// Когда
	Объект = ДесериализаторJson.Десериализовать(Строка, Тип("ТестовыйКлассВложенные"));

	// Тогда
	Ожидаем.Что(Объект).ИмеетТип("ТестовыйКлассВложенные");
	Ожидаем.Что(Объект.ИдЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.НазваниеЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.ОписаниеЧата).Равно(Неопределено);
	Ожидаем.Что(Объект.ПростоеПоле).Равно("значение");

КонецПроцедуры

&Тест
Процедура ДесериализацияКорневогоСоответствия() Экспорт

	// Дано
	JSON = "{""x"": 10, ""y"": 20}";
	Десериализатор = Новый ДесериализаторJson();

	// Когда
	Соотв = Десериализатор.Десериализовать(JSON, Тип("Соответствие"));

	// Тогда
	Ожидаем.Что(Соотв).ИмеетТип("Соответствие");
	Ожидаем.Что(Соотв["x"]).Равно(10);
	Ожидаем.Что(Соотв["y"]).Равно(20);

КонецПроцедуры
